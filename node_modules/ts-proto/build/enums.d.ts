import { Options } from './main';
import { google } from '../build/pbjs';
import { Code } from 'ts-poet';
import EnumDescriptorProto = google.protobuf.EnumDescriptorProto;
import SourceInfo from './sourceInfo';
export declare function generateEnum(options: Options, fullName: string, enumDesc: EnumDescriptorProto, sourceInfo: SourceInfo): Code;
/** Generates a function with a big switch statement to decode JSON -> our enum. */
export declare function generateEnumFromJson(fullName: string, enumDesc: EnumDescriptorProto, options: Options): Code;
/** Generates a function with a big switch statement to encode our enum -> JSON. */
export declare function generateEnumToJson(fullName: string, enumDesc: EnumDescriptorProto): Code;
